using Julo.Network;

namespace SyncGame.Menu
{

    public interface ModeDisplay
    {

        void SetMode(Mode mode, bool isHosted = true);

    } // interface ModeDisplay

} // namespace SyncGame.Menu
